# Runtime dependencies for running the bot against IB Gateway.
# CI installs only the pure-layer deps (numpy pandas pytest pyyaml); the
# broker client is needed only where the bot actually runs.
ib_insync>=0.9.86
PyYAML>=6.0
numpy>=1.26
pandas>=2.0
