# Copy to .env at the repo root (git-ignored) and fill in. Never commit .env.
# The bot only ever reads these from the environment; they are not in any
# config.yaml.
TRADING_MODE=paper
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
